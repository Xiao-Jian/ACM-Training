#include <cstdio>
#include <cstring>
#define MAX 110
using namespace std;

char str[MAX][MAX];

int bfs(int a, int b) {

}

int main()
{
    int n, m;
    while( scanf( "%d%d", &n, &m ) != EOF ) {
        for( int i = 0; i < n; i ++ )
            scanf( "%s", str[i] );

    }
    return 0;
}
